/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateTimeFormatTypePipe } from './DateTimeFormatType.pipe';

describe('Pipe: DateTimeFormatTypee', () => {
  it('create an instance', () => {
    let pipe = new DateTimeFormatTypePipe();
    expect(pipe).toBeTruthy();
  });
});
